---
story_id: "1.3"
title: "Implement Skill Execution Event Capture"
epic: 1
status: "Draft"
created_at: 2026-03-13
created_by: "River (SM)"

clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""

tags:
  - "story"
  - "epic-1"
  - "story-1.3"
  - "instrumentation"
  - "events"
  - "depends-on-1.1"
---

# Story 1.3: Implement Skill Execution Event Capture

## Story Statement

**As a** framework maintainer,
**I want** to capture skill execution events (skill.executed) including skill name, parameters, duration, and result,
**so that** I can identify which skills consume time and which fail frequently.

---

## Acceptance Criteria

1. **Skill Event Emission**
   - When any skill (CLI built-in or custom) is invoked, a `skill.executed` event is emitted
   - Event payload contains: `{event_type: "skill.executed", agent_id, skill_name, parameters (sanitized), duration_ms, status, result_summary}`
   - Skill name uniquely identifies the skill being executed

2. **Event Linking**
   - Skill events are linked to parent agent execution via `execution_id` (same as agent.finished event)
   - Collector can correlate all skills from a single agent execution

3. **Parameter Sanitization**
   - Skill parameters are sanitized (secrets removed) before emission (Story 1.5 handles full sanitization)
   - Common sensitive fields filtered: API keys, tokens, passwords
   - Sanitization adds minimal overhead

4. **Non-blocking Execution**
   - Skill execution is NEVER blocked waiting for event emission
   - Event emitted asynchronously after skill completes
   - Skill error does not prevent event emission

5. **Event Delivery**
   - Event ingestion latency is <100ms in 95th percentile
   - If Collector unavailable, event is queued locally (consistent with agent events)

6. **Status Tracking**
   - Status field is one of: "success", "error", "timeout", "skipped"
   - result_summary contains brief output or error message (sanitized, not full stack trace)

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context
- **Source:** docs/architecture.md#Events, #Data Models
- **Event Type:** skill.executed (added to enum in event schema)
- **Linking:** Via execution_id from parent agent.started/finished
- **Payload:** skill_name (string), parameters (JSONB, sanitized), duration_ms (integer), status (enum), result_summary (string)
- **Transport:** Same HTTP POST to Collector as agent events

### Technical Constraints
- **Sanitization Scope:** Story 1.3 does basic filtering; comprehensive sanitization in Story 1.5
- **Performance:** Event emission must not add measurable overhead to skill execution
- **Integration Point:** Hook into AIOX skill execution pipeline (likely in CLI core)
- **Result Summary:** Max 500 chars to avoid storing large outputs

### Implementation Approach
1. Create middleware/hook for skill execution (wrap AIOX skill dispatcher)
2. On skill start: Record timestamp and skill_name
3. On skill end: Calculate duration, capture status, sanitize parameters and result
4. Emit skill.executed event to EventEmitter (from Story 1.1)
5. Ensure skill execution continues regardless of event emission status

### File Locations
- **Skill Hook:** `packages/aiox-instrumentation/src/skill-hook.js` (new)
- **Hook Registration:** AIOX CLI skill dispatcher (location TBD)
- **Tests:** `packages/aiox-instrumentation/tests/skill-hook.test.js`
- **Integration:** Collector will persist skill.executed events (Story 1.4)

### Testing Requirements
- Unit test: skill.executed event structure validation
- Unit test: Duration calculation accuracy
- Unit test: Parameter sanitization (basic filtering)
- Unit test: Skill error captured without propagation
- Integration test: Skill events linked to parent agent execution
- Performance test: Event emission doesn't block skill execution

---

## Tasks / Subtasks

### Task 1: Create Skill Hook Module
- [ ] Create `packages/aiox-instrumentation/src/skill-hook.js`
- [ ] Implement `SkillHook` class with methods:
  - `wrap(skillFunction)` - Wrapper that emits events before/after skill
  - `_recordStart(skillName, parameters)` - Record skill start time
  - `_recordEnd(result, error)` - Calculate duration and emit event
- [ ] Handle both success and error cases
- [ ] Ensure wrapper doesn't alter skill behavior

### Task 2: Integrate Skill Hook into AIOX CLI
- [ ] Locate AIOX CLI skill dispatcher/executor
- [ ] Register SkillHook wrapper to intercept all skill invocations
- [ ] Ensure skill_name is captured correctly (built-in and custom skills)
- [ ] Pass execution_id context to skill hook (from parent agent event)

### Task 3: Implement Parameter Filtering
- [ ] Create basic sanitization filter in `packages/aiox-instrumentation/src/sanitizer.js` (basic version)
- [ ] Filter common sensitive fields: `api_key`, `token`, `password`, `secret`, `credential`, `auth`
- [ ] Replace values with `[REDACTED]` marker
- [ ] Handle nested objects (recursively filter)
- [ ] Test: Known secrets are filtered

### Task 4: Implement Event Payload Assembly
- [ ] Create function to build skill.executed event payload
- [ ] Validate required fields: event_type, agent_id, execution_id, skill_name, duration_ms, status
- [ ] Extract result_summary (first 500 chars of output or error message)
- [ ] Ensure event schema matches EventEmitter expectations

### Task 5: Write Unit Tests
- [ ] Test: SkillHook.wrap() creates wrapper function
- [ ] Test: Wrapper emits skill.executed event on completion
- [ ] Test: Duration is calculated in milliseconds
- [ ] Test: Status is "success" for successful skills
- [ ] Test: Status is "error" for failed skills
- [ ] Test: Error is captured and wrapped in event (not re-thrown)
- [ ] Test: Execution_id is passed through wrapper
- [ ] Test: Parameters are sanitized before emission

### Task 6: Integration Tests
- [ ] Test: Skill hook integrated into CLI (all skills emit events)
- [ ] Test: Multiple skills in same execution have same execution_id
- [ ] Test: Skill events are correctly ordered (start before end)
- [ ] Test: Collector receives skill.executed events (once Story 1.4 ready)

### Task 7: Documentation
- [ ] Document skill event schema in architecture docs or README
- [ ] Document how to add skill hooks to custom agents/skills
- [ ] Example: Decorating a custom skill with instrumentation

---

## Definition of Done

- [ ] SkillHook module created and tested
- [ ] All CLI skills emit skill.executed events
- [ ] Parameters sanitized (basic filtering)
- [ ] Events linked to parent agent execution
- [ ] No performance impact on skill execution
- [ ] All unit and integration tests pass
- [ ] Documentation updated
- [ ] Ready for comprehensive sanitization (Story 1.5)

---

## Notes

- This story captures skill execution data; full parameter sanitization is handled in Story 1.5
- Collector will store skill events in same table as agent events (with event_type filter)
- Future enhancement: Capture Chain of Thought segments within skill execution (separate story)
- Performance profiling in Story 1.6 will validate overhead <1%

