---
story_id: "1.4"
title: "Build Collector Service & PostgreSQL Storage"
epic: 1
status: "Draft"
created_at: 2026-03-13
created_by: "River (SM)"

clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""

tags:
  - "story"
  - "epic-1"
  - "story-1.4"
  - "backend"
  - "database"
  - "api"
  - "depends-on-1.1,1.2,1.3"
---

# Story 1.4: Build Collector Service & PostgreSQL Storage

## Story Statement

**As a** backend engineer,
**I want** a Node.js/Express collector service that receives events via POST /events, validates them, and persists to PostgreSQL,
**so that** events are stored durably and queryable.

---

## Acceptance Criteria

1. **HTTP Endpoint: POST /events**
   - Collector listens on configurable port (default 3001)
   - Accepts JSON event payload with Content-Type: application/json
   - Returns HTTP 201 with event_id on success
   - Validates event schema before persistence (required fields: event_type, agent_id, timestamp, execution_id)
   - Returns HTTP 400 with error details on validation failure
   - Rejects payloads >10KB (prevents abuse)

2. **Event Validation**
   - Validate required fields present and correct types
   - Validate event_type is in enum: agent.started, agent.finished, error.occurred, recovery.attempt, skill.executed, chain_of_thought
   - Validate agent_id is one of: dev, qa, architect, orchestrator
   - Validate timestamp is ISO 8601 format
   - Validate execution_id is UUID format
   - Invalid events logged and rejected; valid events persisted

3. **Database Persistence**
   - Events inserted into PostgreSQL `events` table
   - Schema matches architecture spec: event_id (UUID), event_type (string), agent_id (string), execution_id (UUID), timestamp (timestamp), duration_ms (integer), payload (JSONB), version (string)
   - Indices created on (agent_id, timestamp DESC), (execution_id), (event_type), (timestamp DESC) for query performance
   - Auto-generated event_id (UUIDs)

4. **Query API: GET /events**
   - Supports filters: ?agent_id=dev&start_time=2026-03-13T00:00Z&end_time=2026-03-13T23:59Z&event_type=agent.finished
   - Returns paginated results: ?limit=100&offset=0
   - Default limit: 100, max: 1000
   - Response: `{events: [...], total: N, limit: 100, offset: 0}`
   - HTTP 200 with events; HTTP 400 if invalid filters

5. **Authentication**
   - All endpoints require Bearer token in Authorization header
   - Token validated against environment variable COLLECTOR_TOKEN
   - Returns HTTP 401 if missing; HTTP 403 if invalid
   - Token validation has minimal overhead

6. **Performance**
   - Can handle 100+ events/second without queueing or loss under normal conditions
   - Query response time <100ms for typical filters
   - Database connection pooling configured (min 5, max 20 connections)

7. **Error Handling**
   - Invalid events logged with request context but don't crash server
   - Database errors logged; requests return HTTP 500 with generic error message (no DB details leaked)
   - Server gracefully handles database unavailability

8. **Health Check**
   - GET /health endpoint returns HTTP 200 with `{status: "ok"}` when operational
   - Used by monitoring/deployment systems

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

---

## Dev Notes

### Architecture Context
- **Source:** docs/architecture.md#Collector Service, #Backend Architecture, #Database Schema
- **Framework:** Express.js 4.18+ with TypeScript
- **Database:** PostgreSQL 14+ with JSONB support
- **Stack:** Node.js 18+, pg (node-postgres client), Pino (structured logging)
- **Port:** Configurable via environment variable PORT (default 3001)

### Database Setup
- **Schema DDL:** See docs/architecture.md#Database Schema for complete table definitions
- **Tables:** events (main), agents (metadata), executions (aggregated), cot_segments (CoT data)
- **Partitioning:** Events table partitioned by timestamp (30-day retention)
- **Indices:** (agent_id, timestamp DESC), (execution_id), (event_type), (timestamp DESC)
- **Cleanup:** Cron job to delete events older than 30 days

### Configuration
- **PORT:** Environment variable, default 3001
- **DATABASE_URL:** PostgreSQL connection string (e.g., postgresql://user:pass@localhost:5432/aiox_ray)
- **COLLECTOR_TOKEN:** Bearer token for authentication
- **LOG_LEVEL:** debug | info | warn | error (default: info)
- **NODE_ENV:** development | production

### File Locations
- **Collector App:** `squads/aiox-ray/collector/src/index.ts` (Express app entry)
- **Routes:** `squads/aiox-ray/collector/src/routes/events.ts` (POST /events, GET /events)
- **Controllers:** `squads/aiox-ray/collector/src/controllers/eventsController.ts`
- **Services:** `squads/aiox-ray/collector/src/services/eventService.ts` (validation, persistence)
- **Database:** `squads/aiox-ray/collector/src/db/client.ts` (connection pool), `queries.ts` (SQL builders)
- **Middleware:** `squads/aiox-ray/collector/src/middleware/auth.ts`, `errorHandler.ts`, `requestLogger.ts`
- **Tests:** `squads/aiox-ray/collector/tests/integration/events-api.test.ts`

### Testing Strategy
- Unit tests: Event validation, error handling
- Integration tests: HTTP endpoints with real/mock database
- Test database: Use docker-compose with test PostgreSQL instance

---

## Tasks / Subtasks

### Task 1: Set Up Express Server & Middleware
- [ ] Create `squads/aiox-ray/collector/src/index.ts` (Express app setup)
- [ ] Add helmet middleware (security headers)
- [ ] Add express.json middleware (body parsing, limit 10kb)
- [ ] Add request logger middleware (Pino)
- [ ] Add CORS middleware (allow localhost for dev)
- [ ] Add rate limiting middleware (1000 req/min)
- [ ] Listen on PORT (default 3001)
- [ ] Add health check endpoint: GET /health → `{status: "ok"}`

### Task 2: Implement Authentication Middleware
- [ ] Create `squads/aiox-ray/collector/src/middleware/auth.ts`
- [ ] Parse Bearer token from Authorization header
- [ ] Validate token against COLLECTOR_TOKEN env var
- [ ] Return HTTP 401 if missing; HTTP 403 if invalid
- [ ] Attach user context to request (for logging)
- [ ] Unit test: token validation logic

### Task 3: Create Database Connection Pool
- [ ] Create `squads/aiox-ray/collector/src/db/client.ts`
- [ ] Use node-postgres (pg) library
- [ ] Configure pool: min 5, max 20 connections
- [ ] Connection timeout: 3 seconds
- [ ] Idle timeout: 10 seconds
- [ ] Graceful shutdown: drain pool on SIGTERM

### Task 4: Implement Event Schema Validation
- [ ] Create `squads/aiox-ray/collector/src/services/validationService.ts`
- [ ] Validate required fields: event_type, agent_id, timestamp, execution_id
- [ ] Validate field types: event_type (string), agent_id (string), timestamp (ISO 8601), execution_id (UUID)
- [ ] Validate enums: event_type ∈ [agent.started, agent.finished, error.occurred, recovery.attempt, skill.executed, chain_of_thought]
- [ ] Validate enums: agent_id ∈ [dev, qa, architect, orchestrator] (or add as needed)
- [ ] Return validation error with details on failure
- [ ] Unit test: Valid events pass; invalid events fail

### Task 5: Implement POST /events Endpoint
- [ ] Create `squads/aiox-ray/collector/src/routes/events.ts` (POST handler)
- [ ] Create `squads/aiox-ray/collector/src/controllers/eventsController.ts`
- [ ] Parse JSON body
- [ ] Validate event using validationService
- [ ] Generate event_id (UUID)
- [ ] Insert into PostgreSQL events table
- [ ] Return HTTP 201 with `{event_id: "...", status: "accepted"}`
- [ ] Handle validation errors: HTTP 400
- [ ] Handle database errors: HTTP 500 (without leaking DB details)
- [ ] Integration test: POST /events accepts valid events

### Task 6: Implement GET /events Query Endpoint
- [ ] Create GET /events handler in events.ts
- [ ] Parse query parameters: agent_id, event_type, execution_id, start_time, end_time, limit, offset
- [ ] Build parameterized SQL query with WHERE clauses
- [ ] Apply indices for performance: (agent_id, timestamp DESC), (execution_id), (event_type)
- [ ] Paginate results: ORDER BY timestamp DESC, LIMIT limit OFFSET offset
- [ ] Return HTTP 200: `{events: [...], total: N, limit: 100, offset: 0}`
- [ ] Handle invalid filters: HTTP 400
- [ ] Integration test: Query with various filters

### Task 7: Create Database Schema & Migrations
- [ ] Create `squads/aiox-ray/collector/src/db/migrations/001-init.sql`
- [ ] Create events table with full schema (see architecture.md)
- [ ] Create agents, executions, cot_segments tables
- [ ] Create all required indices
- [ ] Create partition for current month + next month (for 30-day retention)
- [ ] Create cron job for cleanup (delete events >30 days old)
- [ ] Create migration runner in npm scripts

### Task 8: Error Handling & Logging
- [ ] Create `squads/aiox-ray/collector/src/middleware/errorHandler.ts`
- [ ] Catch validation errors, DB errors, auth errors
- [ ] Log errors with Pino (structured JSON)
- [ ] Return consistent error format: `{error: {code, message, timestamp, requestId}}`
- [ ] NEVER leak database details in error responses
- [ ] Test: Error responses are consistent

### Task 9: Configuration Management
- [ ] Create `squads/aiox-ray/collector/src/config.ts`
- [ ] Load environment variables: PORT, DATABASE_URL, COLLECTOR_TOKEN, LOG_LEVEL, NODE_ENV
- [ ] Validate required variables on startup
- [ ] Provide defaults where appropriate
- [ ] Export config singleton

### Task 10: Write Integration Tests
- [ ] Create `squads/aiox-ray/collector/tests/integration/events-api.test.ts`
- [ ] Test: POST /events with valid event → HTTP 201
- [ ] Test: POST /events with invalid event → HTTP 400
- [ ] Test: POST /events without auth → HTTP 401
- [ ] Test: POST /events with invalid token → HTTP 403
- [ ] Test: GET /events returns events → HTTP 200
- [ ] Test: GET /events filters by agent_id
- [ ] Test: GET /events filters by time range
- [ ] Test: GET /events pagination works
- [ ] Set up test database using Docker Compose

### Task 11: Documentation
- [ ] Create `squads/aiox-ray/collector/README.md`
- [ ] Document how to start collector locally
- [ ] Document required environment variables
- [ ] Document API endpoints with examples
- [ ] Document authentication (Bearer token)
- [ ] Document error responses

### Task 12: Deployment Setup
- [ ] Create `squads/aiox-ray/collector/Dockerfile`
- [ ] Create `docker-compose.yml` for local dev (PostgreSQL + Collector)
- [ ] Document build and run commands
- [ ] Add npm scripts: `dev`, `build`, `start`, `test`

---

## Definition of Done

- [ ] Express server running on port 3001
- [ ] Health check endpoint operational
- [ ] Bearer token authentication working
- [ ] POST /events validates and persists events
- [ ] GET /events queries and returns events
- [ ] Database schema created with indices
- [ ] All unit and integration tests pass
- [ ] Error handling consistent and secure
- [ ] Pino logging configured and working
- [ ] Documentation complete
- [ ] docker-compose.yml works for local development
- [ ] Ready for Dashboard (Story 2.1)

---

## Notes

- Collector is the "hub" that all other components (CLI, Dashboard, Auditor) depend on
- Database retention (30 days for raw events) is critical for compliance; cleanup job must be robust
- Performance targets (<100ms queries) require proper indexing; test with real data volumes
- Error handling must be bulletproof; leaking DB details is a security risk
- This story unblocks Stories 1.5, 1.6, and all of Marco 2 (Dashboard)

